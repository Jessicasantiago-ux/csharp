using System;

public class Exercicio15
{
    public static void Main(string[]args)
    {
        int numero = 8;
        if(numero % 2 == 0)
        {
            Console.WriteLine("O numro é par");
        }
        else
        {
            Console.WriteLine("O numero é ímpar");
        }
    }
}
